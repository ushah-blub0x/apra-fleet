{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/Apra-Labs/apra-fleet/blob/main/memory-contract/v1/schemas/kb_feedback.request.json#",
  "$ref": "#/$defs/v1-kb_feedback-request",
  "$defs": {
    "v1-kb_feedback-request": {
      "type": "object",
      "properties": {
        "repo_remote_url": {
          "type": "string",
          "description": "Origin remote URL of the repo this call is about. Supply it when repo_path is a path on another host (a remote member work folder), where git cannot be shelled out. When omitted, scope is derived from repo_path as before."
        },
        "repo_path": {
          "type": "string",
          "description": "Path to the repo root this call is about. Selects WHICH project KB is read/written. When omitted, falls back to the calling process cwd, which is only correct for single-repo CLI use -- server-handled tool calls must pass it explicitly."
        },
        "id": {
          "type": "string",
          "minLength": 1,
          "description": "ID of the KB entry the feedback applies to"
        },
        "reason": {
          "type": "string",
          "minLength": 1,
          "description": "What was wrong in practice -- appended to the entry content as an ASCII feedback note"
        },
        "role": {
          "type": "string",
          "description": "Role hint for provenance: doer/reviewer/planner/plan-reviewer/kb-agent/harvest/pm/user. Validated server-side against the Author enum; invalid or absent stamps \"unknown\" in the note."
        }
      },
      "required": [
        "id",
        "reason"
      ],
      "additionalProperties": false
    }
  }
}
